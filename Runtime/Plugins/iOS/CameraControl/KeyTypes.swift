/// Values in the range `[CameraServiceKey.min ... CameraServiceKey.max - 1]` are considered valid.
/// `CameraServiceKey.max` is reserved for indicating an invalid key.
public typealias CameraServiceKey = UInt8

/// Values in the range `[CaptureControlKey.min ... CaptureControlKey.max - 1]` are considered valid.
/// `CaptureControlKey.max` is reserved for indicating an invalid key.
public typealias CaptureControlKey = UInt16

/// Values in the range `[CaptureEventInteractionKey.min ... CaptureEventInteractionKey.max - 1]` are considered valid.
/// `CaptureEventInteractionKey.max` is reserved for indicating an invalid key.
public typealias CaptureEventInteractionKey = UInt16
